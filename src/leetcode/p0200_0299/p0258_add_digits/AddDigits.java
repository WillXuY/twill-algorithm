package leetcode.p0200_0299.p0258_add_digits;

public interface AddDigits {
    /**
     * Given an integer num, repeatedly add all its digits until the
     * result has only one digit, and return it.
     *
     * @param num 0 <= num <= 2^31 - 1
     * @return add digits result
     */
    int addDigits(int num);
}
